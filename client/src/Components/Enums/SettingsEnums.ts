import { Layout, Navigation, Template } from "../../Types/settings";

export const LayoutTypes : Array<Layout> = ['grid', 'masonry'];
export const TemplateTypes: Array<Template> = ['classic', 'hover'];
export const NavigationTypes: Array<Navigation> = ['load-more', 'pagination'];